# 当直くん - バージョン履歴

## v6.10.0 (2026-07-22) - 作成者実運用ルール反映（コア配分3件: 学年クォータ制 / ABS-012暦週 / ABS-016日直月1）

> 医局共有ドライブの実資料（手順書2026年度・完成版当直表・作成者の都合表）との照合で確定した
> 実運用ルール差分のうち、回数配分のコア3件を実装。CP-SAT（既定）と Greedy（併走検証用）の
> **両エンジンに同一制約**を実装。目標列・新config項目が無い入力では**完全に従来動作**
> （既存テスト87件が無改変で緑のまま通ることで担保）。

### A. 学年クォータ制（提案#1+#7 / 方針①④）
- Sheet3(コード上sheet4)に**任意列「大学目標」「外目標」**を新設サポート。両列があると有効化し、
  医師別に (大学系B-K割当数 = 大学目標) かつ (総割当数 = 大学目標+外目標) を
  **CP-SATでは等式制約**、Greedyでは `TARGET_CAP[doc]=大学目標+外目標`・
  大学系上限 `UNIV_CAP[doc]=大学目標` として扱う。
- 旧 BASE_TARGET 自動計算＋属性1末尾 EXTRA(+1) は目標列があるとき**全置換**（EXTRA概念なし）。
  ローテ者の回数は月毎の目標入力で変動可能。
- ABS-011「大学系2回まで」固定は医師別 `UNIV_CAP`（既定2）に置換（生成・fix・validate・CP-SAT・独立チェッカー）。
- **プリフライト検算（致命・差分表示）**: `Σ(大学目標+外目標)=総枠数` /
  `Σ大学目標=大学系(B-K)枠数` / `Σ外目標=外病院(L-Y)枠数`。
  **片列だけ存在・一部医師だけ空欄・非数値・負数・氏名突合不能・固定割当>目標も致命**。
- main.py と solver_cpsat.py の二重パースは既存assertに加え
  `quota_enabled` / `大学目標` / `外目標` の完全一致も検証。
- クォータで外目標=0の医師は「外病院0回」ペナルティ/fix対象から除外（大学専任ローテ対応）。

### B. ABS-012 暦週化（方針②）
- 大学系の間隔制約を「ローリング7日間隔」から**「暦週（日曜始まり〜土曜）内で1回まで」**に変更。
  生成フィルタ（collect_candidates/フォールバック）・`is_valid_full_assignment`・
  `validate_absolute_constraints`・fix系（fix_weekly_bg_violations/fix_unassigned_slots ほか）・
  CP-SATモデル（週ごとの AtMostOne）・独立チェッカー verify_output の全経路を更新。
- 月またぎ: 前月末週の大学当直情報は入力に無く判定不能のため、プリフライトに
  「月初第1週（前月から続く暦週）の大学当直は前月末との重複を手動確認」の**警告**を追加
  （月初が日曜のときは新しい週のため警告なし）。

### C. ABS-016 日直の月1回上限（提案#8）
- **日直枠 = 昼系の大学休日（土曜昼C / 日曜昼E / 祝日昼G）＋ 支援日直（J）** を
  医師別に**月1回まで**とする制約を ABS-016 として登録。
  生成・validate・CP-SAT・独立チェッカーに追加（固定割当も回数に含める。固定同士で2回は矛盾エラー）。
- 現行雛形（Tochoku.v7_2026.03.1.xlsx）で CP-SAT 3解とも OPTIMAL・緑バナー維持を確認
  （INFEASIBLEにならないためソフト化は不要）。

### テスト
- `tests/test_grade_quota.py`（12件）を新設。雛形は変更せず、**目標列付きの合成入力**
  （2026-03の暦週4週・医師6人・若手4回/上級3回の傾斜・日直枠3・大学枠8）をopenpyxlで生成し、
  CP-SAT経路で「傾斜配分が実際に出る（総数・大学数とも等式）」「暦週1回」「日直月1」を機械検証。
  致命系（片列のみ/一部空欄/Σ不一致の差分表示）と main.run() エンドツーエンド（サブプロセス・緑バナー）も担保。
- 既存87件は無改変のまま全緑（後方互換の証明）。計99件、約3秒。

### ドキュメント
- `docs/CONSTRAINT_RULES.md`: ABS-011/012改定・ABS-016追加・学年クォータ制のTARGET_CAP注記。
- `docs/TEMPLATE_GUIDE.md`: Sheet3の「大学目標」「外目標」任意列と検算ルールを追記。

## v6.9.0 (2026-07-22) - CP-SAT本統合（既定エンジン化）+ 実行前プリフライト検証 + 氏名突合厳格化

> VERSION 定数を 6.9.0 に更新。既定ソルバーを CP-SAT（厳密解）に切り替え、
> 旧 Greedy+fix パイプラインは併走検証用フォールバックとして残す（1〜2ヶ月の検証後に整理予定・
> **Greedy/fix群のコードは削除しない**）。

### CP-SAT本統合（中期#2 Phase 2）
- **ライブラリ化**: `solver_cpsat.solve(input_data, n_solutions=3, min_diff=6, time_limit=60, verbose=False)`
  を公開。`(data, solutions)` を返す（`data`=InputData、`solutions`=各解の `assign`/status/semi/fair_span/time）。
  CLI（`python3 solver_cpsat.py …`）は従来どおり維持。
- **config.SOLVER 新設**: `"cpsat"`（既定・厳密解）/ `"greedy"`（旧エンジン）。
- **main.run() の分岐**: cpsat選択時は CP-SAT で3解生成 → 各解を main.py の shift_df 同型グリッド
  （`pattern_df`）へ転写 → **既存の evaluate/summary/バナー/Excel出力パイプラインへ合流**。
  出力ファイル名（`<入力名>_v6.9.0.xlsx`）・シート構成（pattern_XX / pattern_XX_summary）は従来と同一。
  使用エンジンは stdout に明示（`🧩 ソルバーエンジン: …`）。
- **二重パース回避の整合性検証（要件5）**: CP-SAT側 `InputData` の `TARGET_CAP`/`EXTRA_ALLOWED` が
  main.py のグローバルと**完全一致することを assert**（不一致時はフォールバック）。
- **自動フォールバック**: ortools未導入（ImportError）/ INFEASIBLE / 例外 / 整合性assert失敗のいずれでも
  警告を出して Greedy エンジンへ自動切替（明示メッセージ）。
- **不変の運用ルール維持**: EXTRA(+1回)対象は属性1優先→名簿末尾（五十嵐・猪股）で従来と一致。
- **実測**: 雛形（32医師・98枠）で3解を約1秒で生成、全パターン緑バナー・全制約違反ゼロ。
- **テスト**: 既存の不変条件テスト（`tests/test_invariants.py` ほか計87件）は cpsat 既定のまま全緑。
  cpsat は `num_patterns` を無視して常に3解生成するため、セッションフィクスチャの実行時間も短縮。

### 氏名突合の厳格化（sheet4前月累積との照合）
- **完全一致のみに変更**: 旧実装の「片側strip×双方向startswith」を撤去。
  同姓医師（佐藤彰/佐藤悠/佐藤勇）で候補が1件に絞れると誤マッチ・誤累積する危険があった。
  `build_prev_name_matcher()` が `normalize_name` 同士の完全一致だけで突合する。
- **config.NAME_ALIASES**（表記ゆれの手動マップ・既定`{}`）を新設。
  マッチ前に適用し、キー・値とも正規化してから引くため「冨田→富田」のどちら向きでも突合可。
- **突合サマリを起動時に必ず表示**: 「一致N人 / 不一致M人」。
  不一致医師は前月累積が0扱いになる旨を明示（`⚠️ 警告`）。

### 実行前プリフライト検証 `preflight_validate()`
- run() の入力読込直後（氏名突合の直後）に呼び出し、「━━ プリフライト検証 ━━」ブロックに集約表示。
- **致命（ValueErrorで停止・メッセージに具体名）**:
  - 必須シート欠落（読込前に停止）
  - sheet1 の日付の重複・期間内の欠落
  - sheet2 の日付の重複
  - **sheet1にあってsheet2(可否)に無い日付**（旧実装は黙って全員「可(1)」フォールバックする
    最悪の静かな壊れ方だった。これを検知して停止）
  - 総枠数 > 全医師の割当理論上限（gap>=3 greedy上限。物理的に埋まらない）
- **警告（続行）**:
  - sheet2 の解釈できないマーク / 列が見つからない医師（既存警告を統合）
  - sheet4 氏名突合の不一致（前月累積0扱い）
  - 祝日と認識されていない平日に「祝日」列名の枠が存在（祝日設定漏れの疑い）
  - sheet2にあってsheet1に無い日付（無視される）

### テスト
- `tests/test_preflight.py`（17件）を新設。氏名突合の同姓プレフィックス非誤マッチ・
  NAME_ALIASES双方向・日付ズレ/重複/欠落/枠数超過の致命・氏名不一致/祝日枠の警告・
  実雛形パス・必須シート欠落停止を担保。

## v6.8.0 (2026-07-22) - 網羅的評価「今すぐ」枠の改善

> 版番号はcolab_duty_scheduler.py(v6.7.0)との交差を解消するため6.6-6.7を飛ばして6.8.0

### 運用安全性
- **祝日の自動取得**: jpholiday（あれば）→内蔵祝日表(2026-2027)→config.HOLIDAYS手動マージの3段。
  従来はconfig手動リスト（既定空）のみで、書き忘れると祝日が平日扱いになっていた。
  起動時に対象月の祝日を `📅` 表示
- **pattern_XXシート先頭に警告バナー**: 違反残存数をExcel単体で明示（黄）/全クリア（緑）。
  ※ヘッダ行が1行下（2行目）に移動
- **総合スコアの0点飽和を解消**: summaryの主表示を raw スコア（マイナス可）に変更し
  パターン間の相対比較を可能に

### アルゴリズム
- **ABS-014（平日/休日偏り）の生成時フィルタを追加**: 残枠で回復不能な偏りのみ候補から排除。
  ABS全クリア0/1000の一因を解消（CP-SAT移行までの暫定改善）

### ドキュメント
- main.pyヘッダの旧Sheet2仕様コメント（「休み希望1,2,3優先度」＝現行と正反対）を訂正
- CONSTRAINT_RULES.mdの実装参照をmain.py正本に修正
- READMEに祝日自動取得を反映

## v6.5.9 (2026-07-21) - 公平性監査に基づく修正（実走テスト+コード監査由来）

### バグ修正
- **公平化処理が固定割当（preassigned）を上書きするバグを修正**
  - `fix_fairness_imbalance` の移動元収集に `is_preassigned_slot()` チェックを追加
  - ユーザーが事前確定した割当が黙って別医師に差し替えられる経路を遮断
- **Sheet2空欄の「月初マーク伝播」を修正**
  - 空欄は常に「1（可）」として扱う（旧: 列の最初のマークが全空欄日に伝播）
  - 解釈できないマーク（非数値・未定義コード）を読込時に警告
- **ABS-013（C-H列カテ当番必須）を `validate_absolute_constraints` に追加**
  - fix関数がABS-013を壊しても「絶対禁忌クリア」と表示される穴を解消

### 公平性の強化
- **公平化の移動先検証を `is_valid_full_assignment` に統合**
  - ABS-010/011/012/013/014 を移動時点で完全チェック
  - safe_fixの全戻し（公平化が丸ごと無効化される問題）の発生頻度を抑制
- **累計（前月+今月）全合計の公平性を可視化**（SOFT-009, summaryに「累計全合計spread」追加）
  - 既定W_FAIR_CUM=0（表示のみ）。EXTRA枠は「若手（名簿末尾）が+1回を担う」運用ルールを維持
  - ※開発中に一時的にEXTRA枠を前月累積最少順へ変更したが、末尾順は意図的な序列（若手優先）
    であることが確認されたため復元。累計均等化を使う場合はconfig.pyでW_FAIR_CUM>0を設定

## v6.0.0〜v6.5.8 の要約（main.pyヘッダコメント／git log から補完）

> この区間は詳細ノートを main.py 冒頭のコメントブロックに保持している。ここでは各版の要点のみ簡潔に列挙する。

| バージョン | 日付 | 要点 |
|-----------|------|------|
| v6.5.8 | 2026-02-06 | ABS-012（大学系7日間隔）を `validate_absolute_constraints` に追加（検証漏れの穴を封鎖） |
| v6.5.6 | 2026-02-06 | SEMI-001の緩和可否を属性で判定（属性1=週1回まで緩和可 / 属性2=ABS-015・カテ表必須）。Sheet4「属性」列を読込 |
| v6.5.5 | 2026-02-06 | カテチーム判定をSheet1:Zのチームコード（A,B,C,D,E等）に限定。属性の"2"等はカテ持ち扱いしない |
| v6.5.4 | 2026-02-06 | SEMI-001（B列カテ表なし）を月曜始まりの週で1回まで許容（週単位追跡） |
| v6.5.3 | 2026-02-06 | SEMI-002をABS-013へ格上げ（C-H列休日大学系カテ当番必須・緩和不可）。summaryを1テーブルに統合 |
| v6.5.2 | 2026-02-06 | カテチーム列検出を改善（カテ当番>属性>カテ>チームの優先順位） |
| v6.5.1 | 2026-02-05 | SEMI違反の誤検出バグ修正（SEMI-001=B列のみ / SEMI-002=C-H列のみに限定） |
| v6.5.0 | 2026-02-05 | Excel構造変更対応（Sheet1:Zカテ当番列 + Sheet4:属性）。大学系ルールを「7日以内2回禁止」に変更。出張前日を自動0扱い |
| v6.4.0 | 2026-02-04 | 大学系週1回ルール（ABS-012）実装。`fix_weekly_bg_violations` 追加 |
| v6.3.0 | 2026-02-04 | 未割当を許容しない段階的ABS緩和（relax_abs=第4段階・ABS-001/006のみ絶対維持）。Excel出力を pattern/summary の2シートに整理 |
| v6.2.0 | 2026-02-04 | 固定割当（事前割当）をABS/SEMI/SOFT全チェックから除外。`is_preassigned_slot()` 追加・全fix関数で移動保護 |
| v6.1.0 | 2026-02-04 | gap>=3上限でTARGET_CAP自動調整。safe_fixにgap/dup個別増加のrevert条件追加。収束ループ後にgap/dup/未割当の最終パス追加 |
| v6.0.5 | 2026-02-04 | CODE_2医師もEXTRA_ALLOWED対象に（枠不足解消）。code_2判定をTARGET_CAPベースに変更 |
| v6.0.4 | 2026-02-04 | 収束ループ後に `fix_unassigned_slots` 最終パス追加（ABS-009を直接補填） |
| v6.0.3 | 2026-02-03 | `safe_fix` ラッパー＋収束ループ（最大3ラウンド）導入。`is_valid_full_assignment` で全ABS統合チェック。最適化を再有効化 |
| v6.0.2 | 2026-02-03 | パターン多様性向上（tie-breakをrandom化）。fix_unassignedにABS-010/011チェック。W_UNASSIGNED=500 |
| v6.0.1 | 2026-02-02 | 段階的制約緩和（全制約→SEMI緩和→HARD緩和→ABS緊急フォールバック）。gap基準をgap>=3に統一 |
| v6.0.0 | 2026-02-02 | 制約体系を全面改定。ABS-007/008/010をSOFT/HARDからABSへ格上げ、ABS-011/013追加。制約ID体系（ABS/HARD/SEMI/SOFT）確立 |
| v4.7 | 2026-01-31 | CONSTRAINT_RULES.md v5.2仕様に基づく整備 |
| v4.3 | 2026-01-30 | C-H列カテ当番制約をソフト化（適格医師不足時のパターン全除外を防止・ペナルティ120は維持） |
| v4.2 | 2026-01-30 | 平日大学系（B,I-K列）の制約緩和（sheet3「1」保有医師は許容）。CC（大型連休特別シフト）を特別カウント化し公平性/バランス計算から除外 |
| v4.1 | 2026-01-30 | 枠決定順序を最適化（C-H列→B,I-K列→L-Y列）。C-H列カテ当番違反をハードチェックに追加 |
| v4.0 | 2026-01-30 | C-H列（休日大学系）カテ当番制約を追加。`fix_ch_kate_violations` 追加（ペナルティ120） |
| v3.9 | 2026-01-30 | print出力の最適化（tqdm進捗バー・テーブル表示）。出力ファイル名にバージョン反映。VERSION定数追加 |
| v3.8 | 2026-01-30 | 外病院最低1回をハード制約化（大学3回以上を防止・ht_0_violations追加） |
| v3.7 | 2026-01-30 | build_hard_constraint_violationsのreturn欠落バグ修正。`fix_code_2_extra_violations` 追加 |
| v3.6 | 2026-01-30 | 可否コード2医師をEXTRA枠（n+1回）対象から除外。CODE_2_DOCTORS導入 |

---

## v3.5 (2026-01-29) - 大学最低1回を準ハード制約に格上げ+制約強化

### 制約の強化
- **大学系最低1回必須を準ハード制約に格上げ**
  - 対象を拡大: 可否コード1.2 → **コード3以外の全医師**
  - コード3は外病院専門なので除外
  - 優先順位を2位に格上げ（TARGET_CAPの直後）
  - 関数名: `fix_code_1_2_violations()` → `fix_university_minimum_requirement()`
  - UNIVERSITY_MINIMUM_REQUIRED_DOCTORSリストを新規作成

- **TARGET_CAP下限チェックを追加**
  - BASE_TARGET未達も違反として検出
  - over-base医師からunder-base医師にシフトを移動
  - 上限超過と下限未達の両方を修正

- **gap違反基準を3日未満に変更**
  - 従来: 4日未満の間隔が違反
  - 新規: 3日未満の間隔が違反
  - より厳格な健康・安全基準

- **EXTRA医師のカテ表コード要件を緩和**
  - B-K列のカテ表コード要件をEXTRA_ALLOWED医師は免除
  - 下位医師の柔軟性を向上

### 修正パイプライン順序の変更
- **新しい順序（厳格な優先順位）:**
  1. ハード制約
  2. TARGET_CAP（上限超過+下限未達）
  3. **大学系最低1回必須（準ハード制約）** ← 新規優先度2位
  4. gap違反（3日未満）
  5. 外病院重複
  6. BG/HT不均衡
  7. 大学3回以上
  8. 大学平日偏り
  9. 公平性

---

## v3.4 (2026-01-29) - TARGET_CAP厳格化+多軸スコアリング+公平性強制

### 新機能
- **多軸スコアリングシステムを実装**
  - 異なる評価軸で最適パターンを選択
  - 3つの評価軸を追加:
    1. **公平性重視**: TARGET_CAP、医師間の割当回数の公平性を最優先
    2. **連続当直回避重視**: gap違反、外病院重複を最優先
    3. **バランス重視**: 大学/外病院バランス、平日/休日バランスを最優先
  - 各軸から最良パターンを1つずつ選択し、合計3パターン出力
  - 同じパターンが複数軸で最良の場合は、総合スコア上位から補填

- **公平性の強制修正機能を追加**
  - `fix_fairness_imbalance()` 関数を実装
  - 最大割当回数と最小割当回数の差を縮める
  - 4回の医師から1回の医師にシフトを移動し、公平性を達成
  - 差が1以下になるまで修正（例: 1回と4回 → 2回と3回）
  - gap制約を守りながら移動先を探索
  - 最適化パイプラインに統合（修正順序の最後に実行）

### 制約の強化
- **TARGET_CAP違反の厳格化**
  - パターン選択時に TARGET_CAP 違反 > 0 のパターンを除外
  - gap違反 > 0 のパターンも除外
  - 未割当 > 0 のパターンも除外
  - ハード制約を満たすパターンのみを候補として選択
  - これにより、TARGET_CAP違反が最終出力に含まれないことを保証

- **公平性制約の強化**
  - active医師間の割当回数の差を最小化
  - n-1回の医師とn+1回の医師が混在する状況を防止
  - 最大200回の試行で公平性を達成
  - gap制約（4日以上の間隔）を守りながらシフトを移動

### 出力の改善
- 各パターンに評価軸ラベルを表示（例: 「公平性重視」「gap回避重視」「バランス重視」）
- TOPパターンのスコア表示に公平性(max-min)を追加
- Excelシートに評価軸ラベルを表示
- 出力説明に多軸評価の詳細を追加

### 技術的詳細
- パターン選択ロジックを2段階に分割:
  1. ハード制約チェック: TARGET_CAP、gap、未割当の違反がないパターンのみ選択
  2. 多軸評価: 各評価軸でソートし、最良パターンを選択
- 各評価軸のソートキー:
  - 公平性: `-cap_violations * 1000`, `-max_minus_min * 100`, `-bg_ht_imbalance * 50`, `raw_score`
  - gap回避: `-gap_violations * 1000`, `-external_dup * 100`, `-hospital_dup * 50`, `raw_score`
  - バランス: `-bg_ht_imbalance * 1000`, `-weekday_weekend_imbalance * 100`, `-bg_over_2 * 100`, `-weekday_over * 100`, `raw_score`
- 公平性修正アルゴリズム:
  1. active医師の割当回数を集計し、最大と最小を計算
  2. 差が2以上の場合、最大の医師から最小の医師にシフトを移動
  3. 移動候補をランダムに選択し、ハード制約とgap制約を満たすか確認
  4. 移動可能な場合、シフトを入れ替え
  5. 差が1以下になるまで繰り返す（最大200回試行）
- 修正パイプライン順序（厳格な優先順位に従う）: ハード制約 → **TARGET_CAP（優先1）** → **gap（優先2）** → **外病院DUP（優先3）** → 1.2 → BG/HT → 大学3+ → 大学平日偏り → 公平性

---

## v3.0 (2026-01-28) - gap違反修正対応

### 新機能
- **gap違反の自動修正機能を追加**
  - `fix_gap_violations()` 関数を実装
  - 4日未満の間隔での割当を検出し、自動的に修正
  - 違反のある割当を4日以上離れた日に移動
  - 最適化パイプラインに統合

### 制約の強化
- 初期パターン生成時にgap違反3以上の候補のみ選択
- 局所探索でgap違反3未満になるswapを拒否
- 候補が見つからない場合のフォールバック機構を追加

### 技術的詳細
- gap違反とは、同じ医師に4日未満の間隔でシフトが割り当てられること
- 修正アルゴリズムは以下の手順で動作:
  1. 全ての医師の割当日を時系列順にソート
  2. 連続する割当間の間隔が4日未満の箇所を検出
  3. 後の割当を別の日（4日以上離れた日）に移動
  4. ハード制約を満たす移動先が見つかるまで試行

---

## v2.9 (2026-01-28) - gap違反フィルタリング

### 制約の追加
- **gap違反フィルタリング制約を実装**
  - 候補選択時にgap違反0,1,2のパターンを除外
  - gap違反3以上のパターンのみを候補として採用
  - 局所探索で gap違反3未満になるswapを拒否

### バグ修正
- 候補ソートロジックを修正（ループ外でソート処理を実行）

---

## v2.8 (2026-01-24) - BG/HTバランス対応

### 新機能
- **大学系と外病院のバランス制約を追加**
  - 大学系（B～K列）と外病院（L～Y列）の割当回数の差が3未満になるよう制約
  - `fix_bg_ht_imbalance_violations()` 関数で最適化後に差3以上を修正
  - 評価関数に差が3以上の場合のペナルティ追加（重み100）

- **可否コード1.2の医師の大学系最低1回制約を追加**
  - `get_avail_code()` 関数で1.2を認識できるよう修正
  - `fix_code_1_2_violations()` 関数で最適化後に大学系0回を修正
  - 評価関数に1.2の医師が大学系0回の場合のペナルティ追加（重み150）

### バグ修正
- **recompute_stats関数のBG/HT範囲を修正**
  - BG: B～G列 → B～K列
  - HT: H～U列 → L～Y列
  - これにより出力Excelの今月/累計の大学合計・外病院合計が正しく計算される

### 機能改善
- **TARGET_CAP違反の強制修正機能を追加**
  - `fix_target_cap_violations()` 関数で最適化後にTARGET_CAP超過を修正
  - 上位医師（小林、及川等）の割当を下位医師（大河内、猪股等）に移動
  - W_CAPペナルティを50→200に強化

- **余り枠の割当ロジックを修正**
  - 昇順ソート→最後のEXTRA_SLOTS人を選択（下位の医師が優先的に+1回を取得）

- **デバッグ情報の追加**
  - +1回対象の医師名を表示
  - TARGET_CAP設定値を表示
  - 可否コード1.2対象医師を表示

- **デフォルトパターン数を100に変更**（環境変数で上書き可能）

---

## v2.7 (2026-01-24) - ハード制約違反の自動修正

### 新機能
- **ハード制約違反の自動修正機能を実装**
  - `fix_hard_constraint_violations()` 関数を追加
  - 局所探索完了後に全ての違反を自動検出・修正
  - 以下の違反を自動修正:
    - カテ表コード保有医師が外病院（L～Y列）に割当
    - 可否コード0の医師が割当
    - 可否コード2の医師がR～Y列に割当
    - 可否コード3の医師がB～K列に割当
  - 代替医師が見つからない場合は未割当として警告表示
  - 修正後にスコアを再評価して最終結果に反映

---

## v2.6 (2026-01-23) - カテ表コードのバグ修正

### バグ修正
- **get_sched_code関数の重大なバグを修正**
  - "0"と"3"を有効なカテ表コードとして扱わないように変更
  - "0"はデータなし、"3"は可否コードであり、カテ表コードではない
  - これにより、カテ表コード保有医師がその日に"0"や"3"の場合、L～Y列への割当が正しく許可される

---

## v2.5 (2026-01-21) - カテ表コード制約の改善

### 制約の修正
- **カテ表コードと列の制約を修正**
  - sheet3で少なくとも1つのカテ表コード（A,B,C,CC,D,E等）を持つ医師を特定
  - カテ表コード保有医師:
    - その日にコードがある場合のみB～K列可、コードがない日は割当なし
    - その日にコードがある場合はL～Y列禁止
  - カテ表コード非保有医師: B～K列に自由に割り当て可能

### 機能改善
- **出力パターンを1個から3個に変更**（TOP3候補を提示）

---

## v2.4 (2026-01-21) - 列構造の変更対応

### 制約の更新
- **列構造の変更対応（B～Y列）**
  - 可否コード2: B～M列 → B～Q列のみ可
  - 可否コード3: H～U列 → L～Y列のみ可
  - カテ表制約: H～U列禁止 → L～Y列禁止

### 新機能
- **B～H列の2回上限制約を実装**
- **診断シートにB～H列2回超過違反検出を追加**

---

## v2.3 (2026-01-21) - カテ表要件のハード制約化

### 制約の強化
- **B～K列のカテ表要件をハード制約に変更**
  - relax_scheduleで緩和不可
  - B～K列カテ表コード欠如違反の検出機能を追加
  - `can_assign_doc_to_slot()` 関数にB～K列カテ表チェックを追加（局所探索でも適用）

---

## v2.2 (2026-01-21) - 初期ハード制約の実装

### 新機能
- **ハード制約違反の検出機能を実装**
  - 可否コード0の医師の割当違反
  - カテ表コード保有医師の外病院割当違反
  - 可否コード2の医師のR～Y列割当違反
  - 可否コード3の医師のB～Q列割当違反

---

---

> **注意**: 以下に旧v3.0時点のスナップショット（制約一覧・パイプライン・パラメータ・ファイル構成）があったが、
> v6.9.0時点の内容と乖離していたため削除した（stale値の整理）。現行の正確な定義は次を参照:
> - **制約・重みの正本**: `docs/CONSTRAINT_RULES.md`（§2.4は `evaluate_schedule_with_raw()` の実適用重み）
> - **既定エンジン・実行フロー**: `README.md` / `docs/CONSTRAINT_RULES.md §6`（CP-SAT既定・Greedyフォールバック）
> - **パラメータ既定値**: `config.py`（SOLVER="cpsat", NUM_PATTERNS=1000, W_FAIR_TOTAL=30, W_UNASSIGNED=500 等）
> - **設計・モジュール分割方針**: `docs/ARCHITECTURE_PLAN.md`
