# backend/ は凍結中（未使用）

このディレクトリ（`app.py` / `scheduler.py` / `requirements.txt`）は
将来のWeb化を想定した試作であり、**現在は未使用・凍結**しています。

- 当直表生成の正本はリポジトリ直下の `main.py`（およびCP-SAT厳密解の `solver_cpsat.py`）です。
- `backend/` 配下のコードは `main.py` の公平性修正（v6.5.9以降）を追従していません。
- Web化を再開するまで、このディレクトリのファイルは編集・使用しないでください。
